<wxs src="../wxs/utils.wxs" module="utils" />

<view
  class="custom-class {{ border ? '' : '' }} {{ utils.bem('tabbar', { fixed, safe: safeAreaInsetBottom }) }}"
  style="{{ zIndex ? 'z-index: ' + zIndex : '' }}"
>
  <slot />
</view>
